---
# =============================================================================
# PR title - Conventional Commits gate
# =============================================================================
# Contributor template only.
#
# The actual workflow implementation is maintained in:
#   alpininsight/.github-private/.github/workflows/reusable-pr-title.yml@main
# =============================================================================

name: PR title

on:
  pull_request_target:
    types: [opened, edited, synchronize, reopened, ready_for_review]

permissions:
  pull-requests: read

jobs:
  validate:
    name: Validate PR title
    uses: alpininsight/.github-private/.github/workflows/reusable-pr-title.yml@main
