---
# =============================================================================
# Alpin Insight workflow template
# Source:
#   - public template: alpininsight/.github/.github/workflow-templates/scheduled-pre-commit-update.yml
#   - private backend: alpininsight/.github-private/.github/workflows/scheduled-pre-commit-update-reusable.yml@48537ce0c297ef27ab6da7da85ec6b8629059abb
# Purpose:
#   - copies a thin pre-commit automation caller into the target repository
#   - keeps org automation logic private and centrally maintained
# When copied:
#   - keep this header so the source remains visible in downstream repos
#   - ensure INSIGHT_TOKEN is available to the repo, preferably via an org-level GitHub Actions secret
# =============================================================================

name: Scheduled pre-commit update (Alpin Insight)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"

jobs:
  autoupdate:
    permissions:
      contents: write
      pull-requests: write
    uses: alpininsight/.github-private/.github/workflows/scheduled-pre-commit-update-reusable.yml@48537ce0c297ef27ab6da7da85ec6b8629059abb
    secrets:
      automation_token: ${{ secrets.INSIGHT_TOKEN }}
