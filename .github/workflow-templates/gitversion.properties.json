{
  "name": "GitVersion (Alpine Insight)",
  "description": "Alpine Insight starter that copies a thin caller to the private GitVersion backend.",
  "iconName": "octicon-tag",
  "categories": [
    "Continuous integration",
    "Deployment"
  ],
  "filePatterns": [
    "^GitVersion\\.yml$"
  ]
}
