---
# =============================================================================
# Alpin Insight workflow template
# Source:
#   - public template: alpininsight/.github/.github/workflow-templates/changelog.yml
#   - private backend: alpininsight/.github-private/.github/workflows/changelog-reusable.yml@48537ce0c297ef27ab6da7da85ec6b8629059abb
# Purpose:
#   - copies a thin caller into the target repository
#   - keeps org workflow logic private and centrally maintained
# When copied:
#   - keep this header so the source remains visible in downstream repos
#   - ensure CHANGELOG_BOT_TOKEN is available to the repo, preferably via an org-level GitHub Actions secret
# =============================================================================

name: Changelog (Alpin Insight)

on:
  push:
    branches: [develop]
    paths-ignore:
      - CHANGELOG.md
  workflow_dispatch:

concurrency:
  group: changelog-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changelog:
    permissions:
      contents: write
      pull-requests: write
    uses: alpininsight/.github-private/.github/workflows/changelog-reusable.yml@48537ce0c297ef27ab6da7da85ec6b8629059abb
    secrets:
      changelog_bot_token: ${{ secrets.CHANGELOG_BOT_TOKEN }}
